# register_discord_message_command

A script to register a message command for the `run-dart-on-discord` artifact, specifically
the "Dart Runner" Discord application.

Run with:

```sh
dart run main.dart
```
